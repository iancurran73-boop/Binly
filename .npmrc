# Render and other Node hosts default to NODE_ENV=production during install,
# which makes npm skip devDependencies. Our build script needs Vite, esbuild,
# Tailwind, PostCSS and friends — all conventionally devDeps. Force npm to
# install them so `npm run build` works in any environment.
include=dev
production=false
